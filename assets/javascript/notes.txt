//**USEFUL CODE**//

  //Stores Data in a JSON property 
  database.ref().set({

  })

  //retrieves data from the database and stores data inside the variable "snapshot"
  database.ref().on("value", function(snapshot) {

    snapshot.val(); 

  });

  //set variables first in Firebase first 
  //set overwrites everything! 
  //example:
  var name = "";
  var age = ""
  var phone = ""

  database.ref().set({
      name: name,
      age: age,
      phone: phone
  });

  //connectionsRef refers to a specific location in our database
  //All connections can be stored like this..
  var connectionsRef = database.ref("/connections"); 

  //This variable is a boolean value that is true if the client is connected 
  var connectedRef = database.ref(".info/connected"); 

  //when the client's connection state changes..
  connectedRef.on("value", function(snap) {

      //if they are connected..
      if (snap.val()) {

          var con = connectionsRef.push(true);

          con.onDisconnect().remove()

      }
  }

  //if Firebase runs into an error, this function will store it into an object
  , function(errorObject) {

      console.log("That read failed" + errorObject.code)

  });